import { resolve } from '@remix-run/router'
import db from '../Components'
import brypt from 'bryptjs'
import jwt  from 'jsonwebtoken'

export const registerService = () =>new Promise(async(resolve , reject) =>{
    try {
        
    } catch (error) {
        reject(error)
    }
})